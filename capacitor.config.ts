import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dk.overmarksgaarden.intra',
  appName: 'Overmarksgaarden Intra',
  webDir: 'out'
};

export default config;
